# Открытые вопросы и план правок

Состояние на 04.09.2026. В репозитории закреплен движок 2.20.0, прод получит его
на следующем `git pull && docker compose up -d --build`; голова `e2e_rnnt`,
перезапусков ноль. Один баг движка ждёт отправки апстриму — он первым в списке ниже,
дальше то, что осталось незакрытым, в порядке от «может укусить» к «долги помельче».

Закрытые пункты не удаляются, а помечаются датой: по ним потом восстанавливается,
почему сделано именно так.

## 1. WebM/Opus с заголовком не 48 кГц распознаётся тишиной

Движок берёт частоту из контейнера Matroska, а не из декодера Opus, который всегда
отдаёт 48 кГц. Файл, собранный `ffmpeg` из 16-килогерцового источника, проходит с
кодом 200 и пустым текстом (`tokens=0 words=0` в логе), а тот же поток Opus в OGG
распознаётся полностью. Воспроизводится на 2.18.0, 2.19.0 и 2.20.0
([замер](research/head-choice-and-wer.md)):

```sh
ffmpeg -i example.wav -c:a libopus -f webm bad.webm   # заголовок 16 кГц — тишина
ffmpeg -i example.wav -ar 48000 -c:a libopus -f webm ok.webm  # заголовок 48 кГц — текст
```

**Что делать:** отправить апстриму с этим же воспроизведением. Нас это не задевает —
браузеры пишут 48 кГц, а консоль вообще собирает WAV, — но задевает всякого, кто
конвертирует записи сам.

## 2. Промежуточные дельты потока на файловой загрузке почти пусты

При `stream=true` на загруженном файле до `done` доходит одна-две дельты: движок
получил весь звук сразу и стримить ему нечего. Замеры апстрима про поток сделаны на
живом WebSocket с чанками по 100 мс — это другой сценарий.

**Что делать:** ничего, пока никто не просил живой поток. Если попросят — смотреть в
сторону родного `/v1/ws` движка и ручки `--stream-max-window-secs`, а не в наш фасад.

## 3. MP3, склеенный через `cat`, распознаётся только до конца первого потока

Обнаружено при замере предела загрузки: `cat a.mp3 a.mp3 > double.mp3` даёт файл,
который `ffprobe` видит целиком (6420 с), а движок читает до конца первого потока —
текст вышел байт в байт равным одиночному файлу, 35 434 символа вместо 70 734.
Правильно склеенный через `-f concat` и перекодированный файл распознаётся полностью.

**Что делать:** знать об этом, если кто-то будет объединять записи. Отправлять
апстриму пока не с чем: спорно, обязан ли декодер идти дальше первого потока, когда у
второго свой заголовок. Нас это не задевает — консоль ничего не склеивает.

## 4. Буст 5.0 ещё не встречался с живой диктовкой

`HOTWORDS_BOOST` вернулся к штатным 5.0 вместе с движком 2.17.0, и 140 фраз
глоссария впервые влияют на декодирование в полную силу — до этого он либо не
работал вовсе, либо стоял на единице. На тестовых записях вреда нет: глоссарий,
чьих слов в записи нет, оставляет текст побайтово тем же.

**Что делать:** ничего, пока не появится жалоба. Если в тексте начнут всплывать
слова из списка там, где их не говорили, опускать `HOTWORDS_BOOST` шагами (5 → 3 →
1), а не менять голову. Как именно это выглядит, видно на замере с бустом 20: движок
собрал `Blackwird` из услышанного `Bird` и глоссарного `Blackwell` — см.
[исследование](research/head-choice-and-wer.md).

## 5. Копию глоссария всё равно нужно забирать руками

Кнопка «Экспорт» в консоли есть, но регулярность на человеке: список из полутора
сотен фраз живёт в томе `data/` на проде, и другого экземпляра у него нет.

**Что делать:** забирать файл после заметных правок. Автоматической выгрузки по
расписанию нет и, пока список меняется раз в месяц, не нужна.

## 6. Не проверено то, что мы отдаём наружу

Выключено по умолчанию и ни разу нами не запускалось: очередь для длинных файлов
(`ENABLE_JOBS=1`), диаризация через проброшенные родные эндпоинты движка.
Из этого списка 04.09.2026 вышли двое: встроенный словарь брендов (проверен и включен
по умолчанию) и `VAD=1` (проверен и оставлен выключенным - см. "Закрыто").

**Что делать:** проверять по мере надобности, по одному, с записью результата
сюда. Раньше времени включать нечего — но помнить, чем обернулась непроверенная
`ml_ctc_large` (пункт в «Закрыто»): предлагать в интерфейсе то, что мы сами не
запускали, значит не знать его требований к памяти.

## 7. Замерить параллельные окна, когда выйдет релиз движка с #328

Апстрим принял предложение о параллельном декодировании окон (см. "Закрыто" за
05.09.2026), флаг `--file-window-concurrency N` лежит в main движка, но релиза с ним
пока нет - последний 2.20.0. Пока обновить нечего, а решать по чужим цифрам мы не
будем.

**Что делать, когда релиз выйдет:** прогнать `bench/` на этой машине и на рабочей
установке, serial против concurrency 2, обязательно с `POOL_SIZE=2` или
`--batch-pool-size 1` - иначе второго слота в пуле нет и файл все равно идет
последовательно, то есть замер покажет ничью и мы сделаем неверный вывод. Сравнивать
время и WER по словам, и только после этого решать, добавлять ли флаг в
`console/engine.py` и настройку в консоль.

Помнить о цене: пул на две сессии стоит около 20 МиБ и делит потоки энкодера между
слотами, а автор предупреждает о потере 10-20% на одиночном запросе. Для диктовки
одиночный запрос - основной сценарий, поэтому эту потерю надо увидеть в цифрах, а не
предположить.

## 8. Тело загрузки идет в движок целиком, а не потоком

`read_upload` поднимает файл с диска в `bytes` и держит его, пока движок не ответит.
На замере предела это стоило 1058 МиБ памяти консоли при гигабайтной загрузке -
ровно размер файла, - и вместе с копией в движке дало пик 2786.7 МиБ из 2861
доступных ([замер](decisions.md)). Из-за этого предел и пришлось опустить до 500 МБ:
уперлись мы в память, а не в скорость.

Потоковая пересылка сняла бы консольную половину расхода: временный файл, который
Starlette и так пишет на диск, отдавался бы движку кусками. Двух мест это касается -
`upload_duration_seconds` нужен только заголовок, его придется читать с начала потока,
а не из готовых байт, и `/api/test` в `console/api.py`, который эти же байты
использует второй раз.

**Что делать:** ничего срочного. 500 МБ - это 4.5 часа записи одним файлом, дальше
предела никто пока не подходил. Браться стоит, если понадобится вернуть гигабайт или
поднять `POOL_SIZE` выше единицы. Обратите внимание: вывод "проксировать потоком
смысла нет" из [замера длинной диктовки](research/head-choice-and-wer.md) был про
время - накладные консоли там 0.1%, - и памяти он не касается.

## Закрыто

- **05.09.2026 - последовательные окна: предложение принято апстримом.** Отправлено
  как [#327](https://github.com/ekhodzitsky/gigastt/issues/327) с нашими числами:
  на восьми ядрах при `POOL_SIZE=1` файл 264.6 с шел двенадцатью окнами по
  1050-1180 мс энкодера и ~45 мс жадного декодирования, состояние декодера между
  окнами не переносится, значит окна уже независимы. Автор реализовал
  `--file-window-concurrency N` (переменная `GIGASTT_FILE_WINDOW_CONCURRENCY`, по
  умолчанию 1) в [PR #328](https://github.com/ekhodzitsky/gigastt/pull/328), слито в
  main 04.09.2026. Релиза с этим флагом пока нет, последний - 2.20.0. На своей машине
  (M1 Pro) автор получил ускорение в 1.5-1.7 раза по времени при свободном втором
  слоте пула, текст совпал с последовательным. Наш замер - в пункте 7 выше: чужие
  цифры мы в выводы не переносим.

- **04.09.2026 - `PR_SET_PDEATHSIG` движку не нужен.** Вопрос возник из осиротевшей
  тестовой заглушки: убитый сигналом KILL pytest оставляет `fake_gigastt serve` жить.
  На проде такого риска нет - контейнер собран с `init: true`, поэтому смерть консоли
  завершает tini, контейнер останавливается вместе с движком, а `restart:
  unless-stopped` поднимает все заново. Осиротевшая заглушка после KILL по самому
  pytest - неудобство разработки, а не риск прода, и отдельной ручки в `engine.py` не
  заводим.

- **04.09.2026 - предел загрузки замерен и опущен до 500 МБ.** Гигабайт на рабочей
  установке стоял непроверенным; проверили файлом из настоящей речи. 1000 МиБ
  проходят: код 200, 1206.7 с, 355 015 знаков, - но пик памяти контейнера 2786.7 МиБ
  при потолке 2861, то есть 75 МиБ до `OOMKilled`. 500 МиБ дают 600.8 с и пик 1716.0
  МиБ. Порог "безопасно" задавался до замера как 2.5 ГиБ; прошло только 500, оно и
  выставлено на рабочей установке. Умолчание в коде остается 150. Числа и разбор, из
  чего складывается пик, - в [решениях](decisions.md).

- **04.09.2026 - VAD проверен и оставлен выключенным.** Критерий задавался до замера:
  выигрыш не меньше 15% на записях 3-10 минут И расхождение текста не больше 1% слов.
  По времени вышло -21.5% и -29.5%, по тексту 3.07% и 3.44% смысловыми словами, а на
  короткой диктовке VAD еще и медленнее (+19% на девяти секундах). Умолчание не
  меняется, ручка остается. Заодно закрыт вопрос "медленной склейки": склейка не
  медленная, она линейная, и накладные консоли - 0.1% на десятиминутном файле.
  Подробности - в [замере](research/head-choice-and-wer.md) и
  [решениях](decisions.md).

- **04.09.2026 - встроенный словарь брендов проверен и включен.** A/B на девяти
  настоящих записях диктовки (27 минут, 2654 слова, голова `e2e_rnnt`, боевой
  глоссарий и буст 5.0) дал одно различие на весь набор: "mascod" стало "маскот",
  то есть словарь исправил слово, а не выдумал новое. Задержка не изменилась.
  Поэтому `HOTWORDS_DEFAULT=1` стало умолчанием, а не осталось необязательной
  ручкой. Заодно закрыт молчаливый дефект: до этого флаг из `.env` не доходил до
  движка, потому что кнопка "Развернуть" не посылала это поле и сервер брал его из
  старого состояния. Числа и поименные различия - в
  [исследовании](research/head-choice-and-wer.md).

- **18.08.2026 — уборку кеша графов забрал движок.** Наш
  [#287](https://github.com/ekhodzitsky/gigastt/issues/287) исправлен в 2.18.0:
  `cache-gc` оставляет граф каждой установленной головы и снимает только
  устаревшие `*_optimized.onnx`. Руками сверять `/proc/*/maps` больше не нужно,
  команда есть в [ручной проверке](manual-smoke.md), а что именно проверено при
  обновлении — в [решениях](decisions.md).

- **11.08.2026 — метрики и глоссарий доведены.** Счётчик файлов за всё время
  переживает перезапуск, задержка меряется по всему пути (приём загрузки, движок,
  отдача), в консоли видны последние файлы поимённо, а `p95` молчит, пока замеров
  меньше десяти, вместо того чтобы показывать середину выборки. У глоссария
  появились импорт, экспорт и добавление по одной фразе; форматирование целиком на
  сервере. Микрофон при работе не по HTTPS теперь честно сообщает, почему он
  недоступен, — саму поддержку HTTPS оставили на будущее по решению владельца.
- **11.08.2026 — выбор головы подтверждён владельцем.** «Мы остаёмся на лучшей,
  e2e_rnnt сейчас выглядит как топчик».

- **11.08.2026 — `ml_ctc_large` измерена, и замер оказался нужнее, чем ожидалось.**
  Голова предлагалась в интерфейсе, но ни разу нами не запускалась. По WER она
  встала между `rnnt` и `e2e_rnnt` (6.25% против 6.56% и 4.32%), при этом вдвое
  медленнее и пишет так же голо, — брать незачем, кроме случая, когда нужны пять
  языков. Главное же: её первый запуск берёт **2538 МиБ**, то есть упёрся бы в
  наш тогдашний потолок 2 ГБ и был бы убит ровно в момент сборки графа. Потолок
  поднят до 3 ГБ. Заодно сверены размеры весов в каталоге — заявленные цифры
  завышали `ml_ctc_large` на 56 МБ, поправлено на измеренные.
  Подробности — в [исследовании](research/head-choice-and-wer.md).
