# Runtime dependencies of the console, pinned for reproducible images.
# Tests run from pyproject.toml via `uv sync`; this file is only for the container.
fastapi==0.140.13
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
starlette==1.3.1
