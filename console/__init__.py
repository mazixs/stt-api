"""Console for the GigaSTT speech recognition engine."""

# Единственное место, где живёт номер версии: `pyproject.toml` читает его отсюда, и
# отсюда же он попадает в схему на `/api/openapi.json`. Правила — семантические:
# ломающие изменения меняют старшую цифру, новые возможности — среднюю, исправления —
# младшую. Что вошло в каждую версию, перечислено в `CHANGELOG.md`.
__version__ = "1.1.1"
